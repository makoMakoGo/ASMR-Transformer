# ASMR Transformer Context

本文档记录 ASMR Transformer 的领域语言。后续 issue、PR、架构评审和重构应优先使用这里的词，避免同一个概念在代码和讨论中反复改名。

## Core Domain

**语音转文字工具** 是整个应用。用户提供音频，应用调用 ASR 转录得到原始文本，再可选调用 LLM 润色生成更易读的文本。

**音频来源** 是一次转录运行的输入来源，分为本地音频和在线音频。

**本地音频** 是用户从浏览器选择或拖拽上传的音频文件。它已经是浏览器可上传给 ASR 的 `File`。

**在线音频** 是用户粘贴的远程链接。它可能是 AList 播放页，也可能是真实音频直链。

**AList 播放页** 是受支持 ASMR 站点上的展示页面，需要通过 AList `/api/fs/get` 解析出真实音频链接。

**真实音频链接** 是可被服务端代理下载的最终音频 URL。它可能来自用户直链，也可能来自 AList 解析结果。

**远程音频来源策略** 是判断在线音频是否受支持的一组规则，包括 AList 播放页域名、真实下载域名、音频扩展名、MIME 映射和私网拦截规则。

**远程音频检查** 是在线音频转录前的元信息检查。它返回文件名、大小、类型和用户可展示的信息。

**远程音频代理** 是服务端从真实音频链接拉取音频并流式返回给浏览器的路径。它负责域名校验、大小限制、断开中止和响应头转发。

**ASR 转录** 是把音频发送给 SiliconFlow 兼容的语音识别接口并得到原始文本的过程。

**转录运行** 是用户点击开始转录后的一次完整运行。它覆盖音频准备、上传/下载进度、等待识别、成功、空结果、错误、取消和日志。

**原始文本** 是 ASR 转录直接返回的文本，未经过 LLM 润色。

**LLM 润色** 是把原始文本发送给 OpenAI 兼容模型，并通过 SSE 流式返回润色文本的过程。

**润色运行** 是用户点击开始润色或重新润色后的一次完整运行。它覆盖请求构造、流式增量、解析 warning、完成原因、错误、取消和日志。

**润色指令** 是用户可配置的文本处理规则。默认目标是纠错、补标点、分段排版，并保持原意。

**设置持久化** 是 WebUI 读取和写入服务器 `.env` 的能力。它负责默认值、trim、env key 映射、保存、重新加载和放弃改动。

**ASR 运行设置** 是 ASR 转录实际需要的设置切片：ASR API Key、ASR API URL、ASR 模型。

**LLM 运行设置** 是 LLM 润色实际需要的设置切片：LLM API URL、LLM 模型、LLM API Key、润色指令。

**运行日志** 是页面展示的过程记录，用来告诉用户当前发生了什么，包括信息、成功、警告和错误。

## Workflow Vocabulary

一次常规使用流程：

1. 用户配置 ASR 运行设置和可选的 LLM 运行设置。
2. 用户选择本地音频，或输入在线音频并执行远程音频检查。
3. 用户启动转录运行。
4. 应用得到原始文本。
5. 用户启动润色运行。
6. 应用得到润色文本。

## Architecture Notes

当前仓库没有已记录的 ADR。未来影响架构方向的决策应写入 `docs/adr/`。

架构评审中应使用上述领域词汇命名 deep Module。这里的 deep Module 是架构评审术语，不是 Domain Module 的缩写。例如说“转录运行 Module”，不要只说某个 hook 文件名；说“远程音频来源策略”，不要把 AList 域名规则和真实下载域名规则当成两个无关概念。
